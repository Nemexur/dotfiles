---
create_paths:
  - path: "{{ home_prefix }}/.gnupg"
    state: directory
    mode: '0700'
  - path: "{{ home_prefix }}/.config"
    state: directory
    mode: '0755'
  - path: '{{ home_prefix }}/Library/Application Support/Code/User'
    state: directory
    mode: '0755'
